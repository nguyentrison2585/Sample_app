<%= link_to gravatar_for(current_user, size: Settings.users_avatar_size),
  current_user %>
<h1><%= current_user.name %></h1>
<span><%= link_to t("view_my_profile"), current_user %></span>
<span><%= t "post_size", post_size: @current_user.microposts.size %></span>
